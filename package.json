{
  "name": "blaze-it-monorepo",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "frontend",
    "backend",
    "contracts"
  ],
  "scripts": {
    "build": "echo 'Vercel build disabled. Use Cloudflare Pages/Workers.' && exit 0"
  }
}
