{
  "name": "blaze-it-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Blaze It - World mini app",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js --network worldchain-sepolia",
    "verify": "hardhat verify --network worldchain-sepolia"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "hardhat": "^3.0.6"
  }
}
